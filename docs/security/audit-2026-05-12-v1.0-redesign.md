# Security Posture Report - v1.0 Redesign (Sprint 4 ecosystem refactor)

**Status:** APPROVED for v1.0 tag
**Date:** 2026-05-12
**Auditor:** security-reviewer (Claude Code agent profile)
**Scope:** the 14 commits between v0.3-beta-pre-redesign and the v1.0 redesign tip
       (commit range main..feat/s4-redesign-foundation, HEAD c26d9716)
**Companion to:** docs/security/audit-2026-05-12-v1.0.md (pre-redesign v1.0 audit; still applies)

## Findings summary

| # | Area | Severity | Status |
|---|---|---|---|
| A-1..A-5 | Pattern path PII surface | - | PASS |
| B-1..B-3 | Cloud Function side | - | PASS |
| C-1..C-2 | Token economy | - | PASS |
| D-1..D-4 | PII in logs | - | PASS |
| E-1 | Domain purity | - | PASS (whitelisted exception only) |
| F-1..F-2 | Copy-rule compliance | - | PASS |
| G-1 | Feature flag rollback | informational | PASS-with-doc-gap (v1.0.1 follow-up) |
| H-1 | Dependencies | - | PASS (no new direct deps) |
| I-1 | Secret scan | - | PASS |

No Critical, High, or Medium findings. One informational item (G-1).

## A. Pattern path PII surface

- **A-1 PASS** - Grep of apps/mobile/lib/features/pattern_engine/ for analyzeMoodText|analyzePatterns|gemini|generateContent|Gemini returned zero matches. Engine is fully client-side; no proxy reachable from the trigger path.
- **A-2 PASS** - Datasource upserts via result.toJson() only at apps/mobile/lib/features/pattern_engine/data/datasources/patterns_firestore_datasource.dart:37. The entity is defined at apps/mobile/lib/features/pattern_engine/domain/entities/pattern_result.dart:22-36 with exactly 8 fields: dateId, mannKendallZ, slidingNegCount, consecutiveHighIntensity, zScoreToday, cusumC, triggeredTier, schemaV. No text field, no moodCode field, no intensity field.
- **A-3 PASS** - firebase/firestore.rules:156-207 enforces the allowlist via request.resource.data.keys().hasOnly([...8 keys...]) on create (lines 163-166) and diff().affectedKeys().hasOnly([...6 mutable keys...]) on update (lines 188-191). Doc-id regex ^\d{4}-\d{2}-\d{2}$ at lines 160 and 185. Delete denied at line 206.
- **A-4 PASS** - apps/mobile/test/features/pattern_engine/data/datasources/patterns_firestore_datasource_test.dart:33-42 asserts json.keys.toSet() equals exactly the 8 allowlisted keys; date regex pinned at line 95. Drift detection in place.
- **A-5 PASS** - firebase/test/firestore_rules.test.ts:794-938 describe block Sprint-4 patterns/{dateId} rule (ADR-0011) covers: off-allowlist text denial (lines 850-858), off-allowlist moodCode denial (lines 860-868), bad doc-id regex (lines 830-838), mismatched dateId (lines 840-848), tier-allowlist (lines 870-878), bounds checks (lines 880-898), null-tier permit (lines 900-910), same-day overwrite (lines 912-924), delete denial (lines 926-937).

## B. Cloud Function side

- **B-1 PASS** - functions/src/analyzePatterns.ts:47-51 defines NEGATIVE_MOOD_CODES = new Set([...]) with exactly the codes sad, angry, anxious. The code okay is NOT in the set; the comment at lines 43-46 cites ADR-0010 reclassification of okay to positive (sign +1).
- **B-2 PASS** - Grep of functions/src/analyzePatterns.ts for cheerUpEvents returned zero matches. Only functions/src/sendCheerUpPush.ts:111 references the path, and only as the onDocumentCreated trigger source. analyzePatterns CF cannot fan out to interventions.
- **B-3 PASS** - Flag default false confirmed at:
  - apps/mobile/lib/main.dart:65 -- intervention_dispatch_enabled: false in rc.setDefaults.
  - apps/mobile/lib/app/feature_flags.dart:34 -- interventionDispatchEnabled: false in FeatureFlags.defaults().
  - apps/mobile/lib/features/garden/presentation/controllers/cheer_up_controller.dart:67-77 -- early-return when flag disabled; no anchor writes, no event-doc create. sendCheerUpPush therefore has no triggering document creation in v1.0.

## C. Token economy

- **C-1 PASS** - Grep of apps/mobile/lib/features/tokens/ for mood_type|mood_entry|mood_score|MoodType|MoodEntry|MoodScore returned zero matches. Award path is mood-agnostic by construction; awardForLog(userId:) at apps/mobile/lib/features/tokens/data/datasources/token_balance_firestore_datasource.dart:36 takes only the userId.
- **C-2 PASS** - Token balance fields live on the users/{uid} profile doc. The outer rule firebase/firestore.rules:9-10 (allow read, write: if isOwner(uid)) constrains writes to the owner. Cross-user spoofing prevented; the Firestore transaction at token_balance_firestore_datasource.dart:42-65 re-reads under the owner auth context.

## D. PII in logs

- **D-1 PASS** - apps/mobile/lib/features/mood/presentation/controllers/log_mood_controller.dart:261-265 emits a warning of the form pattern_engine_save_failed dateId=<id> failure=<runtimeType>. No userId, no triggeredTier, no mood content. Catch-block at line 270 logs only failure=<runtimeType>.
- **D-2 PASS** - Same file, line 292: logger.warn(token_award_failed failure=<runtimeType>). No userId, no balance, no award value. Catch-blocks at lines 298, 300 log only the failure runtimeType.
- **D-3 PASS** - apps/mobile/lib/features/garden/presentation/controllers/cheer_up_controller.dart:68-71 logs the literal cheer_up_dispatch_skipped with data: const {reason: flag_disabled} only. Other warns at lines 91, 96, 105 emit static strings; no userId, no reason text leak.
- **D-4 PASS** - Grep of apps/mobile/lib/features/ for print( or debugPrint( returned zero matches. All logging routes through packages/core/lib/src/logger.dart.

## E. Domain purity

- **E-1 PASS** - Grep of apps/mobile/lib/features/*/domain/ for package:flutter|package:firebase_|package:cloud_firestore returned exactly one match: apps/mobile/lib/features/settings/domain/services/day_night_strategy.dart:14 -- import package:flutter/material.dart show ThemeMode. This is the ADR-0010 Compliance-Check whitelisted exception, with a comment block at lines 1-13 routing it through the architect. No other violations.

## F. Copy-rule compliance

- **F-1 PASS** - apps/mobile/test/features/harvest/copy_audit_test.dart:34-46 defines forbidden lexicon: delete, clear, reset, lost, destroyed, gone, erased, wilted, wilting, dead, dying. Coverage matches the brief verbatim. Test walks lib/features/harvest/ recursively (line 54), excludes generated .freezed.dart and .g.dart, applies whole-word case-insensitive match inside string literals (line 97).
- **F-2 PASS** - apps/mobile/lib/features/harvest/presentation/weekly_summary_screen.dart:30-32 defines the locked banner constant verbatim from CLAUDE.md Pre-approved phrasing: Your garden this week has been harvested and saved to your history. A new week begins -- a fresh canvas for your story. Rendered at line 73 via _HarvestBanner(text: harvestBanner).

## G. Feature flag rollback

- **G-1 INFORMATIONAL** - docs/runbooks/feature-flag-rollback.md documents ai_pattern_analysis_enabled and gemini_detection_enabled in the Flags-in-scope table (lines 14-19) but does NOT mention intervention_dispatch_enabled. The new flag is correctly defaulted in code and gated in cheer_up_controller, but the runbook lacks a row for it. Not a tag blocker -- the flag intended state for v1.0 is default-false-never-flipped-on, and the runbook existing procedure (Console flip then Publish, propagation within 60 minutes) applies unchanged. Flag for v1.0.1 doc-only patch: add a row with default false, blast radius = legacy cheer-up dispatcher path, Sprint-5 plan = flip to true after Quote Library + Bipolar disclaimer land.

## H. Dependencies

- **H-1 PASS** - git diff main..HEAD on apps/mobile/pubspec.yaml, packages/*/pubspec.yaml, and functions/package.json returned EMPTY diffs (verified via git diff --stat; no pubspec or package.json files appear in the change list). The 14-commit redesign added 13,600 lines of new code under existing dependency surface only. Previous v1.0 audit docs/security/audit-2026-05-12-v1.0.md covered the dependency tree; that audit HIGH/CRITICAL clearance carries forward unchanged.

## I. Secret scan

- **I-1 PASS** - Grep of apps/mobile/lib/features/ and functions/src/ for AIza[A-Za-z0-9_-]{16,} or sk-[A-Za-z0-9]{20,} or gemini_api_key or service_account or private_key or client_secret returned zero matches. Gemini key continues to flow through functions/src/geminiClient.ts GEMINI_API_KEY (Secret-Manager-backed); no new key materials in the redesign branch.

## Sign-off

APPROVED for v1.0 tag.

Recommend the orchestrator merge feat/s4-redesign-foundation to main and tag v1.0. The single informational item (G-1, runbook doc gap) is a v1.0.1 doc-only follow-up, not a code-side blocker -- the flag itself defaults to the safe (off) state in both main.dart and feature_flags.dart, and the cheer-up controller honours it.
