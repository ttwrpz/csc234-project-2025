<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Biometric (S3 carry-over): USE_BIOMETRIC for API 28+,
         USE_FINGERPRINT for legacy < 28. Required by local_auth + the
         on-device biometric demo path. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>
    <!-- FCM (S5 5.5b): Android 13+ requires runtime POST_NOTIFICATIONS
         consent before notifications are delivered. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Daily check-in (self-set local reminder via
         flutter_local_notifications zonedSchedule). RECEIVE_BOOT_COMPLETED
         lets the plugin's boot receiver re-arm the scheduled reminder
         after a reboot. SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM are
         declared for parity with the plugin's exact-alarm path; the daily
         check-in itself uses inexactAllowWhileIdle so it does not depend
         on the (Android 13+ restricted) exact-alarm runtime grant. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- v1.6 - mood entry attachments via image_picker.
         CAMERA: required to launch the system camera intent (camera
         apps refuse the intent without this declaration even though
         no runtime permission is requested by image_picker itself).
         READ_MEDIA_IMAGES + READ_MEDIA_VIDEO: Android 13+ replaced the
         broad READ_EXTERNAL_STORAGE with per-mime-type permissions.
         READ_EXTERNAL_STORAGE: still required on Android ≤12 for the
         pre-Photo-Picker gallery flow; maxSdkVersion=32 lets newer
         devices opt into the OS Photo Picker (which needs no perm). -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <application
        android:label="MoodBloom"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- FCM defaults - channel id matches sendCheerUpPush payload
             (functions/src/sendCheerUpPush.ts). Single channel per OQ-12. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@android:color/holo_orange_light" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="cheer_up" />
        <!-- flutter_local_notifications scheduled-notification receivers
             for the self-set daily check-in (zonedSchedule). The boot
             receiver re-arms the daily reminder after a reboot / app
             update; the plain receiver fires the scheduled alarm. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
